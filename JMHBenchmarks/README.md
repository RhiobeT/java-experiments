Benchmarks made with JMH:
* BenchStreamVsListSum checks the performance of streams and lists when performing a sum
* BenchStreamVsListIO checks the performance of streams and lists when reading a file and filtering the results